<h2><%= @category.name %></h2>

<h4>This is everything listed under <%= @category.name %> so far:</h4>
<ul>
<% if @items %>
  <% @items.each do |item| %>
    <li><%= link_to  item.name, item_path(item) %></li>
  <% end %>
<% end %>
</ul>