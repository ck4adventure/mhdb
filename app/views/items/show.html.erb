<h2><%= @item.name %> </h2>
<h4>Stats</h4>
<% if @item.item_stat %>
  <%= render partial: "items/stats/stats_table" %>
<% elsif @item.trap_item? %>
  No stats yet. But it needs some.
  <br>
<% else %>
  This item is used as/for <%= @item.category.name %>.
  <br>
<% end %>

Limited Edition? <%= @item.le ? "Yep" : "Nope" %> </p>






<br>
<br>
<% if current_user %>
  <%= link_to  "Edit this item", edit_item_path(@item) %>
<% else %>
  <p>Please sign in if you'd like to help update this item's info.</p>
<% end %>
<br>
<br>

<%= link_to  "Back to items", items_path %>
