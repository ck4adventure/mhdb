<h2><%= @location.name %></h2>

<h4>Region</h4>

It is found within<%= link_to  @location.region.name, region_path(@location.region) %>

<h4>Rank</h4>

This location is unlocked once you are a <%= link_to  @location.rank.title, rank_path(@location.rank) %>

<br>
<br>

<h4>Mice</h4>
<% if @location.mice %>
  <%= render partial: "mouses/mice_table", locals: { mice: @location.mice } %>
<% else %>
  <p>There are no mice listed just yet.</p>
<% end %>

<br>
<% if current_user %>
  <%= link_to  "Edit this location", edit_location_path(@location) %>
<% else %>
  <p>Please sign in if you'd like to help update this location</p>
<% end %>


<br>
<br>
<%= link_to  "Back to locations", locations_path %>