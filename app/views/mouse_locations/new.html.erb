<h2>Add a mouse location</h2>
You are currently add a location for the <%= @mouse.name %> mouse.

<form id="mouselocation_add_form" action="<%= mouse_locations_path(@mouse) %>" method="POST">
  <input type="hidden" value=<%= form_authenticity_token %> name="authenticity_token">
  <%= select(:mouselocation, :location_id, options_for_select(@mouse.non_locations_options)) %>
  <%= submit_tag("Add Location") %>
</form>

<%= render 'shared/errors' %>
<br>
<br>
<%= link_to  "Take me to home", root_path %>